description = "List all connected GitHub repositories (sources)"
prompt = """List my connected jules sources (GitHub repositories).
Use the jules_list_sources tool.

Sources are GitHub repositories connected to jules. The source name format is:
`sources/github/owner/repo`

After listing, if the user wants to create a task, they can use `/jules:create`."""
