description = "Show a summary of all active (non-completed) sessions"
prompt = """Show a summary of all active jules sessions.

1. Use jules_list_sessions to get all sessions
2. Filter for sessions that are NOT completed or failed (active states):
   - QUEUED
   - PLANNING
   - AWAITING_PLAN_APPROVAL
   - AWAITING_USER_FEEDBACK
   - IN_PROGRESS

3. Group and summarize by state using these visual indicators:
   - 🟠 Awaiting Approval (offer to approve)
   - 🟢 In Progress (no action needed)
   - 🟡 Needs Feedback (offer to send message)
   - ⏳ Queued / Planning

4. For each active session, show:
   - Status Icon (🟢, 🟠, 🟡, ⏳)
   - Session ID
   - Title
   - Current state
   - Suggested next action"""
