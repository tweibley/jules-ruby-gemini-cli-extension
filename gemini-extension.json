{
  "name": "jules",
  "version": "1.0.0",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "julesServer": {
      "command": "node",
      "args": ["${extensionPath}${/}mcp-server${/}dist${/}jules.js"],
      "cwd": "${extensionPath}",
      "env": {
        "JULES_API_KEY": "${JULES_API_KEY}"
      }
    }
  }
}
